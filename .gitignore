# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# visual review (Visual UI workflow)
/.screenshots/
/.dev-server.log
/.dev-server.err.log

# local screenshot dropbox (raw incoming visual references — copies
# that should ship live in design-reference/ with explicit naming)
/screenshots/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# interni reference dokumenty (studie, brand, logo) - nikdy do gitu
/Analysis/

# Claude Code lokalni nastaveni session
/.claude/settings.local.json

# Lokalni dev skilly a session logy (negitovat) - pridano 2026-05-20
# Pluginy a marketplaces jsou v settings.local.json (viz vyse), nic z nich nelezi v repu.
/.claude/skills/humanizer/
/.claude/skills/prompt-master/
/.claude/skills/brand-voice-generator/
/.claude/commands/sessions/
/.claude/sessions/
/.claude/TOOLING.local.md

# lokalni zaloha CLAUDE.md (negitovat)
/CLAUDE.backup.md

# zdrojove originaly fotek tymu — do repa jdou jen optimalizovane v public/team/
/Photos/
