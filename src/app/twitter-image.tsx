// Twitter card image reuses the OpenGraph image — same canvas, same copy.
// Re-exporting keeps a single source of truth.

export {
  default,
  alt,
  size,
  contentType,
} from "./opengraph-image";
